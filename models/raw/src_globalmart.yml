version: 2

sources:
    - name: globalmart
      description: a clone of the Snowflake Database 
      database: raw
      schema: globalmart
      tables:
        - name: customers
        - name: orders
          description: raw orders table
          columns:
            - name: orderid
              tests:
                - unique
                - not_null
        - name: products
